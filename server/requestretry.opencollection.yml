# OpenCollection (https://opencollection.com) file for the RequestRetry mock server (server/index.js).
# Import into Bruno (https://www.usebruno.com) — File > Import Collection > Open Collection Format.
opencollection: "1.0.0"
info:
  name: RequestRetry Mock Server
  summary: >-
    Endpoints exposed by the Node mock server in server/ that the RequestRetry Android app's
    Ktor client talks to. The server always succeeds — client-side failure is injected by the
    app's ApiClient/ScenarioHolder, not here.
  version: "1.0.0"
  authors:
    - name: Cleiton Monteiro
      email: cleitonmonteiro.dev@gmail.com
request:
  variables:
    - name: baseUrl
      value: http://localhost:8090
items:
  - info:
      name: Get Profile
      type: http
      seq: 1
    http:
      method: GET
      url: "{{baseUrl}}/profile"

  - info:
      name: Orders
      type: folder
      seq: 2
    items:
      - info:
          name: Create Order
          type: http
          seq: 1
        http:
          method: POST
          url: "{{baseUrl}}/orders"
          headers:
            - name: Content-Type
              value: application/json
            - name: X-Operation-ID
              value: 0e26f9fd-2c17-47bf-89b7-47c54ec897ea
            - name: Idempotency-Key
              value: 4e3d1cc1-3ecb-4335-89e4-0fae2328dd72
          body:
            type: json
            data: |-
              {
                "item_name": "Mechanical keyboard",
                "quantity": 2,
                "customer_name": "Ada Lovelace"
              }

  - info:
      name: Verify Order Operation
      type: http
      seq: 3
    http:
      method: GET
      url: "{{baseUrl}}/operations/0e26f9fd-2c17-47bf-89b7-47c54ec897ea"
