package io.github.cleitonmonteiro.requestretry.data.remote

/**
 * The mocked Node server started from `server/` (see repo root). `10.0.2.2` is the Android
 * emulator's alias for the host machine's loopback interface; a physical device needs the
 * host's LAN IP instead.
 */
internal const val BASE_URL = "http://10.0.2.2:8090"
